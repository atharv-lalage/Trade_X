// This file is no longer used for stock data.
// Real stock data is now fetched from the backend API via /api/quotes
// Default watchlist symbols are defined in ../services/stockApi.js

// Kept for backward compatibility — remove when no component imports from here.
export const watchlist = [];
